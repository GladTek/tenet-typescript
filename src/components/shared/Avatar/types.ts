

export interface AvatarProps {
    image: {
        src: string;
        alt?: string;
    };
    direction?: string;
    url: string;
}

export interface AvatarImageProps {
    image: {
        src: string;
        alt?: string;
    }
    direction?: string;
}